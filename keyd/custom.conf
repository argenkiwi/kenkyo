# The custom layer is meant to provide a place where you can add macros, shortcuts and
# special keys that might be relevant to your workflow. You can access it by holding the
# space-bar and immediately tapping `b`. The layer will remain active until the space-bar
# is released.

[ids]

*

[main]

space = timeout(overloadt2(fumbol, space, 200), 500, space) 

[fumbol]

b = swap(custom)

[custom]

q = f1
w = f2
e = f3
r = f4
t = f5
y = f6
u = f7
i = f8
o = f9 
p = f10
leftbrace = f11
rightbrace = f12
backslash = f13
a = timeout(overloadt2(meta, f14, 200), 500, f14)
s = timeout(overloadt2(shift, f15, 200), 500, f15)
d = timeout(overloadt2(control, f16, 200), 500, f16)
f = timeout(overloadt2(alt, f17, 200), 500, f17)
g = f18
h = f19
j = timeout(overloadt2(alt, f20, 200), 500, f20)
k = timeout(overloadt2(control, f21, 200), 500, f21)
l = timeout(overloadt2(shift, f22, 200), 500, f22)
semicolon = timeout(overloadt2(meta, f23, 200), 500, f23)
apostrophe = f24
# Your macros, shortcuts and special keys here.
